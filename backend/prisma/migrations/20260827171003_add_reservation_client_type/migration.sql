BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[reservations] ADD [clientType] NVARCHAR(1000) NOT NULL CONSTRAINT [reservations_clientType_df] DEFAULT 'PARTICULAR';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
