BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[guests] ADD [photoUrl] TEXT;

-- AlterTable
ALTER TABLE [dbo].[users] ADD [photoUrl] TEXT;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
